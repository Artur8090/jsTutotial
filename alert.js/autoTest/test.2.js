describe("pow", function () {
    it("любое число в степени 0 дает 1", function () {
        assert.equal(pow(10, 0), 1);

    });

});

describe("NaN", function() {
    it("ноль(0) в нулевой степеньи дает NaN")
    assert.equal(isNaN(0,0), "дает NaN")
})